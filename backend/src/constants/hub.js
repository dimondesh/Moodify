export const HUB_MIN_TRACKS = 51;
/** Preview size returned in GET /hubs/:id section payloads. */
export const HUB_SECTION_LIMIT = 12;
/** Max entities stored per section (preview + show-all source). */
export const HUB_STORE_LIMIT = 50;
export const HUB_CANDIDATE_POOL = 400;
export const HUB_PREVIEW_COUNT = 3;
