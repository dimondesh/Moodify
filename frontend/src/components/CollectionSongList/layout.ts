/** Shared desktop table layout so header and rows stay column-aligned. */
export const desktopSongListGridClass =
  "grid grid-cols-[16px_minmax(0,4fr)_minmax(0,2fr)_7rem] items-center gap-x-4";

export const desktopSongListPaddingClass = "px-4 sm:px-6 md:px-10";

/** Actions column: save | duration | menu — clock aligns over duration. */
export const desktopSongListActionsClass =
  "ml-auto grid grid-cols-[1.75rem_2.5rem_1.75rem] items-center gap-x-1";
